# yaml-language-server: $schema=https://www.schemastore.org/github-workflow.json
---
# This workflow is auto-generated by Radius. It deletes a Radius application: it
# detects whether the selected GitHub Environment is wired for Azure or AWS and
# calls the matching reusable delete workflow (delete-azure.yml / delete-aws.yml)
# with resource_type=application. Deleting runs on an ephemeral k3d control plane
# that restores the persisted Radius state, runs `rad app delete` (including any
# recipe deletes against the target cluster), and persists the updated state again.
name: Radius - Delete Application

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'GitHub Environment name'
        required: true
        default: 'aks-dev'
      application:
        description: 'Name of the application to delete'
        required: true
      force:
        description: 'Force the delete even if resources are stuck in a non-terminal state (may orphan external resources)'
        required: false
        type: boolean
        default: false

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  # Bind to the GitHub Environment so environment-scoped variables are visible, then
  # pick the provider from whichever identifying variable is set. A job that calls a
  # reusable workflow (`uses:`) can't bind an environment itself, so this routing
  # decision has to happen in a regular job first.
  detect:
    name: Detect provider
    runs-on: ubuntu-24.04
    environment: ${{ inputs.environment || 'aks-dev' }}
    outputs:
      provider: ${{ steps.detect.outputs.provider }}
    steps:
      - name: Determine provider from environment variables
        id: detect
        run: |
          if [ -n "${{ vars.AZURE_CLIENT_ID }}" ]; then
            echo "provider=azure" >> "$GITHUB_OUTPUT"
          elif [ -n "${{ vars.AWS_ROLE_ARN }}" ]; then
            echo "provider=aws" >> "$GITHUB_OUTPUT"
          else
            echo "No AZURE_CLIENT_ID or AWS_ROLE_ARN set on this environment." >&2
            echo "provider=none" >> "$GITHUB_OUTPUT"
            exit 1
          fi

  azure:
    name: Azure
    needs: detect
    if: ${{ needs.detect.outputs.provider == 'azure' }}
    uses: ./.github/workflows/delete-azure.yml
    with:
      environment: ${{ inputs.environment || 'aks-dev' }}
      resource_type: application
      name: ${{ inputs.application }}
      # A workflow_dispatch boolean input arrives as the string "true"/"false",
      # including its default, but delete-azure.yml/delete-aws.yml declare `force`
      # as `type: boolean`. Compare to coerce it, so every delete (forced or not)
      # passes a real boolean. See actions/runner#1483.
      force: ${{ inputs.force == 'true' }}
    secrets: inherit